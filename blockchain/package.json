{
  "name": "docutrust-blockchain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:document-notary:amoy": "hardhat run scripts/deploy-document-notary.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^16.4.5",
    "hardhat": "^2.24.3"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
