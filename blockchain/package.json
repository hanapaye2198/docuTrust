{
  "name": "docutrust-blockchain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy-document-notary.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^16.4.5",
    "hardhat": "^2.24.3"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
