PORT=3001

# Polygon anchoring (all three required for POST /anchor and full POST /verify)
POLYGON_RPC_URL=https://rpc-amoy.polygon.technology
POLYGON_PRIVATE_KEY=
DOCUMENT_NOTARY_ADDRESS=

# Local dev only: start HTTP server without Polygon or CSC (endpoints return 503 until configured).
# DOCUTRUST_ALLOW_EMPTY_SERVICE=true

# CSC remote signing (alternative to Polygon – see deploy docs for full variable list)
